version: '3'
services:

  myapp:
    container_name: myapp
    image: myapptest
    
  config:
    container_name: configmanager
    image: configmanagertest

  proxy:
    container_name: proxy
    image: proxytest
    ports:
    - "8000:8000"
    depends_on:
    - myapp

