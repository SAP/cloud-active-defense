import { OnlyValidRespondPropertyDirective } from './only-valid-respond-property.directive';

describe('OnlyValidRespondPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyValidRespondPropertyDirective();
    expect(directive).toBeTruthy();
  });
});
