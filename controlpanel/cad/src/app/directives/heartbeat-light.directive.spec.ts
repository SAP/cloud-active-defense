import { HeartbeatLightDirective } from './heartbeat-light.directive';

describe('HeartbeatLightDirective', () => {
  it('should create an instance', () => {
    const directive = new HeartbeatLightDirective();
    expect(directive).toBeTruthy();
  });
});
