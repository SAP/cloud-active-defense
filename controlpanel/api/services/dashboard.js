
module.exports = {
    /**
     * Return dashbord customize by user based on `id`
     */
    getDashboard: async (userId) => {
        try {
            //TODO
        } catch(e) {
            throw e;
        }
    },
    /**
     * Update customized dashboard by user based on `id`
     * @param {number} id 
     * @param {Object} dashboard 
     */
    updateDashboard: async (userId, dashboard) => {
        try {
            //TODO
        } catch(e) {
            throw e;
        }
    }
}