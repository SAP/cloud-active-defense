
module.exports = {
    /**
     * Returns user info based on `id`
     * @param {number} id
     */
    getUserInfo: async (id) => {
        try {
            //TODO
        } catch(e) {
            throw e;
        }
    },
    /**
     * Create a new user
     * @param {Object} user 
     */
    createUser: async (user) => {
        try {
            //TODO
        } catch(e) {
            throw e;
        }
    },
    /**
     * Update user based on `id`
     * @param {number} id
     * @param {Object} user
     */
    updateUser: async (id, user) => {
        try {
            //TODO
        } catch(e) {
            throw e;
        }
    },
    /**
     * Return list of users
     */
    getUsers: async () => {
        try {
            //TODO
        } catch(e) {
            throw e;
        }
    },
    /**
     * Change user's password based on `id`
     * @param {number} id 
     * @param {string} password
     */
    changeUserPassword: async (id, password) => {
        try {
            //TODO
        } catch(e) {
            throw e;
        }
    },
    /**
     * Upload profile picture of user based on `id`
     * @param {number} id
     * @param {string} path
     */
    uploadUserPicture: async (id, path) => {
        try {
            //TODO
        } catch (e) {
            throw e;
        }
    },
    /**
     * Delete user based on `id`
     * @param {number} id 
     */
    deleteUser: async (id) => {
        try {
            //TODO
        } catch(e) {
            throw e;
        }
    },
    /**
     * Update roles for user based on `id`
     * @param {number} id 
     * @param {Array} roles 
     */
    updateUserRoles: async (id, roles) => {
        try {
            //TODO
        } catch(e) {
            throw e;
        }
    }

}