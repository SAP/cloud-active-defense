
module.exports = {
    /**
     * Calculate statistics of how many decoys were triggered from `date` to now at each interval 
     * @param {number} date
     */
    calculateStatisticsOfAlertsFromDate: async (date) => {
        try {
            // Todo
        } catch(e) {
            throw e;
        }
    },
}