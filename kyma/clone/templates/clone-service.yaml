apiVersion: v1
kind: Service
metadata:
  name: clone-service
  namespace: {{ .Values.namespace }}
  labels:
    app: clone  
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 2000
  selector:
    app: clone
